<h1>Top page</h1>
<%= link_to '新規投稿', '/posts/new' %>   <%#左記の続きでmethod: :GET が付くがGETの場合は基本省略でOK%>
<% @posts.each do |post| %>
  <div class="post">
    <div class="post-date">
      投稿日時：<%= post.created_at %>
    </div>
    <div class="post-content">
      <%= post.content %>
    </div>
  </div>
<% end %>
